#ifndef MAXIMUM_H   //we can name it anything we want
#define MAXIMUM_H   //these lines are just to make sure that the header file is 
					//not included twice

					//pragma once does the same thing as these two lines



int maximum(int a, int b);

#endif // !maximum.h